{
    "name": "nz_fish_database_tool",
    "description": "Tool for converting the NIWA nz fish database into a set of files suitable for import to google my maps",
    "require": {
        "symfony/console": "^4.0",
        "markbaker/complex": "^1.4"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "bin": [
        "fish"
    ],
    "require-dev": {
        "phpunit/phpunit": "^6.2",
        "mockery/mockery": "^1.2"
    }
}
